# Direct runtime dependencies (the source of truth, human-edited).
# The pinned + hashed lockfile that actually gets installed is requirements.lock;
# regenerate it after editing this file with:
#   pip-compile --generate-hashes --output-file requirements.lock requirements.txt
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
pydantic>=2.13.5
pydantic-settings>=2.15.0
httpx>=0.28.1
python-multipart>=0.0.32
itsdangerous>=2.2
resend>=2.44.0
fastembed>=0.8.0
numpy>=2.5.3
email-validator>=2.3.0
mcp>=2.2.0
authlib>=1.8.0
# Imported directly by app/oidc.py (JWKS parsing + id_token signature) and by
# tests/fakeidp.py. They arrive via authlib today, but "we import it" is what
# decides whether a dependency is declared: authlib shipped its own authlib.jose
# until recently, so the day it changes JOSE backends an undeclared joserfc
# becomes ModuleNotFoundError at import of app/oidc.py.
joserfc>=1.7.5
cryptography>=50.0.1
ldap3>=2.9.1
